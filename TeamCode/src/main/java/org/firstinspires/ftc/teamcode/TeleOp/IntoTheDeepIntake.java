package org.firstinspires.ftc.teamcode.TeleOp;

import com.qualcomm.robotcore.eventloop.opmode.LinearOpMode;
import com.qualcomm.robotcore.hardware.DcMotor;
import com.qualcomm.robotcore.hardware.Servo;

public class IntoTheDeepIntake {
    private Servo intakeMotor;
    private final double forwardPosition = 0.5;
    private final double reversePosition = -0.5;

    while()

    {
        double Servo;
        if (gamepad1.left_trigger) {
            Servo = forwardPosition;
        } else - if (gamepad1.right_trigger) {
        Servo = reversePosition;
    }
    }
}

